# --- Coeur RAG ---
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-huggingface>=0.1
langchain-chroma>=0.2
langchain-text-splitters>=0.3
chromadb>=0.5

# --- Embeddings (CPU) ---
sentence-transformers>=3.0
torch>=2.2 ; platform_system != "never"  # roue CPU par défaut sur Windows

# --- Chargement documents ---
pypdf>=5.0
docx2txt>=0.8
openpyxl>=3.1
pandas>=2.0

# --- API Web ---
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.0
requests>=2.31

# --- Base de donnees (phase ulterieure) ---
# psycopg2-binary>=2.9
