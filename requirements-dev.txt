# Outils de développement et de qualité (pas nécessaires pour faire tourner
# l'assistant, seulement pour le vérifier).
#
# Installation :  venv\Scripts\python.exe -m pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0          # tests automatisés
pytest-cov>=5.0      # mesure de la couverture de tests
ruff>=0.6            # analyse statique + formatage (remplace flake8, isort, black)
pre-commit>=3.7      # exécute les vérifications avant chaque commit
